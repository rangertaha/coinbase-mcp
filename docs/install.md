# Install

```sh
go install github.com/rangertaha/coinbase-mcp/cmd/coinbase@latest
```

This produces a `coinbase` binary. Or build from source:

```sh
git clone https://github.com/rangertaha/coinbase-mcp
cd coinbase-mcp
make build        # produces ./bin/coinbase
```
